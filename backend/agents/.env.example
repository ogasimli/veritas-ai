# ==============================================================================
# Veritas AI Agent System Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your values
# Required variables are marked with [REQUIRED]
# Optional variables have sensible defaults

# ==============================================================================
# Gemini API Configuration
# ==============================================================================

# [REQUIRED] Gemini API key for accessing Google AI models
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# ==============================================================================
# Model Aliases (Optional - Override Default Models)
# ==============================================================================

# [OPTIONAL] Model used for reasoning-heavy tasks
# Default: gemini-3-pro-preview
# Used by: Numeric validation, cross-table detection, logic consistency,
#          disclosure verification, reviewers
# GEMINI_PRO_MODEL=gemini-3-pro-preview

# [OPTIONAL] Model used for lightweight classification and aggregation tasks
# Default: gemini-3-flash-preview
# Used by: Document validator, disclosure scanner, external signal aggregator
# GEMINI_FLASH_MODEL=gemini-3-flash-preview

# Note: Deep Research uses "deep-research-pro-preview" (not configurable)

# ==============================================================================
# Agent Mode Selection (Optional)
# ==============================================================================

# [OPTIONAL] Select which agent to run in isolation
# Default: orchestrator (runs the full multi-agent pipeline)
# Options:
#   - orchestrator        (default) Full pipeline with all agents
#   - numeric_validation  Run only numeric validation agent
#   - logic_consistency   Run only logic consistency agent
#   - disclosure_compliance  Run only disclosure compliance agent
#   - external_signal     Run only external signal agent
#
# This is useful for:
# - Testing individual agents in isolation
# - Debugging specific validation pipelines
# - Running agent-specific benchmarks
#
# VERITAS_AGENT_MODE=orchestrator

# ==============================================================================
# Vertex AI Configuration (Optional - For Enterprise Deployments)
# ==============================================================================

# [OPTIONAL] Use Vertex AI instead of Gemini API
# Set to 1 to enable Vertex AI, 0 or unset to use Gemini API
# Vertex AI provides:
# - Enterprise SLAs and support
# - Data residency controls
# - Private networking options
# - Unified billing with GCP
#
# GOOGLE_GENAI_USE_VERTEXAI=0

# [REQUIRED if GOOGLE_GENAI_USE_VERTEXAI=1] Google Cloud project ID
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# [REQUIRED if GOOGLE_GENAI_USE_VERTEXAI=1] Google Cloud region
# Available regions: us-central1, us-east4, europe-west1, asia-southeast1
# GOOGLE_CLOUD_LOCATION=us-central1

# ==============================================================================
# Authentication Notes
# ==============================================================================

# Gemini API (Default):
#   - Requires GEMINI_API_KEY
#   - No additional authentication needed
#   - Rate limits: https://ai.google.dev/pricing
#
# Vertex AI:
#   - Requires GOOGLE_GENAI_USE_VERTEXAI=1
#   - Local: gcloud auth application-default login
#   - Production: Service account with Vertex AI User role
#   - No API key needed (uses GCP authentication)

# ==============================================================================
# Production Deployment Notes
# ==============================================================================

# ADK Playground (Local Testing):
#   make playground
#   - Opens web UI at http://localhost:8501
#   - Requires GEMINI_API_KEY
#
# Cloud Run Deployment:
#   make deploy
#   - Deploys agent to Cloud Run with web UI
#   - Set GEMINI_API_KEY in Cloud Run environment variables
#   - Or configure Vertex AI with service account permissions
#
# Backend Integration:
#   - The backend installs this agent package as a dependency
#   - Backend uses its own DATABASE_URL and GCS_BUCKET
#   - Agent configuration is read from this .env file
